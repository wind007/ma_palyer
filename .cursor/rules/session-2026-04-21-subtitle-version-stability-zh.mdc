---
description: 2026-04-21 字幕与多版本播放稳定性修复记录（中文）
alwaysApply: false
---

# 今日工作记录（字幕 + 多版本播放稳定性）

## 背景
- 用户反馈：  
  - 首页/播放链路改动后，字幕功能异常（无法关闭、默认不显示、切换不生效）。  
  - 多版本 + 字幕组合下出现播放器初始化失败。

## 关键故障现象
- 播放器报错：`invalid or unsupported media`。
- 日志显示播放 URL 中携带了 `SubtitleMethod=None` 时，部分媒体源直接打开失败。

## 根因归纳
- 关闭字幕语义与“未显式选择字幕”的语义混淆，导致初始化阶段误传 `SubtitleMethod=None`。
- 字幕切换链路混用（外挂注入与重建流）导致状态不一致。
- 多版本下音轨/字幕索引可能与当前 `MediaSource` 不匹配，形成不合法参数组合。

## 最终落地方案

### 1) URL 参数安全收敛（`lib/services/emby_api.dart`）
- `AudioStreamIndex` / `SubtitleStreamIndex` 仅在当前 `MediaSource` 中确实存在时才下发。
- 无效索引自动回退并记录警告日志，避免把非法参数喂给播放器。
- 关闭字幕时不再强制下发 `SubtitleMethod=None`，改为跳过字幕参数，让服务端走默认策略（避免媒体打开失败）。

### 2) 播放器初始化字幕策略（`lib/pages/video_player_page.dart`）
- 初始化时区分三种场景：
  - 明确字幕索引（>=0）：按字幕轨初始化；
  - 外部明确传入 `-1`：视为主动关闭；
  - 其余场景：不强制覆盖，交给服务端默认。
- 初始化完成后，若存在默认字幕索引，主动执行一次字幕应用，降低“参数已带但轨道未挂载”的概率。

### 3) 播放器内字幕切换稳定化（`lib/pages/video_player_page.dart`）
- 切换字幕采用“双通道兜底”：
  - 先尝试外挂字幕注入（`setExternalSubtitle`）；
  - 若未激活字幕轨，自动回退到重建播放流切换。
- 关闭字幕走重建流路径，确保“关得掉”。

### 4) 详情页外部预选（`lib/pages/video_detail_page.dart`）
- 多版本改为可预选，点击版本卡只切换“当前版本”。
- 增加“默认字幕（进入播放前生效）”选择区域（按版本记忆）。
- 进入播放器时携带版本/字幕偏好，减少进入后二次操作。

## 本轮结果
- 初始化失败问题已消失（可正常播放）。
- 字幕开启/切换/关闭链路恢复可用。
- 多版本场景下参数组合更稳健，降低回归风险。

## 回归建议
- 单视频单版本：默认字幕显示、切换、关闭、再开启。
- 单视频多版本：每个版本分别设置默认字幕并进入播放验证。
- 切换版本后再切字幕，确认不出现初始化失败或轨道失效。
- 异常源验证：无字幕源、单字幕源、多字幕源、外挂字幕不可用源。
