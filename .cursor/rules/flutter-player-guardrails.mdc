---
description: Flutter 播放器多平台护栏：依赖、启动、构建、跨平台工作流与规则一致性
alwaysApply: true
---

# Flutter 播放器护栏规则

本仓库为**多平台**应用（Windows、Android、iOS、iPadOS、macOS、Linux、Web 等）。以下各节在每次改动时一并遵守；若本文件内条目与其它说明存在冲突，以**更严格、更不利于单平台走捷径**的表述为准，并在回复中简要说明取舍。

## 所有改动（通用，强制）
- 任何与本项目相关的改动（代码、`pubspec`、原生工程、脚本、工具链/规则配置等）都必须：**不损害其他平台**（先评估影响面，用最小改动与必要抽象/条件编译避免连带破坏），并 **验证确实生效**（用可重复的 `flutter analyze`、`flutter build`、运行或与本次改动直接相关的关键路径确认实际结果，禁止仅凭代码阅读或未运行假设）；无法在当前环境验证的平台须在交付说明中写明未验证原因与用户侧验证方式。

## 依赖变更
- 不要自动升级或降级 `fvp` 或 `video_player`。
- 在修改媒体相关依赖前，先复现当前错误，并说明兼容性影响。
- 任何可能触发原生 SDK 下载或工具链变更的依赖升级，都必须先征求确认。
- 新增或删除 `pubspec` 直依赖前，扫描工程内实际引用，避免冗余；传递依赖仍可能出现在锁文件中，属正常现象。

## 启动安全（`lib/main.dart`）
- 保留 `fvp.registerWith(...)`，除非用户明确要求重构。
- 未经确认，不要移除或更改全局初始化顺序（`ensureInitialized`、各管理器初始化、然后 `runApp`）。
- 对启动配置的任何修改都必须包含一次编译/运行验证步骤。

## 构建产物与生成文件
- 绝不要提交或有意编辑生成目录/文件，例如 `.dart_tool/`、`build/`、`**/flutter/ephemeral/`、`GeneratedPluginRegistrant*`。
- 优先修复忽略规则与源代码；不要把生成产物当作真实来源（source of truth）。

## 安全工作流
- 复现问题 -> 定位根因 -> 最小修复 -> 使用 `flutter pub get` 与平台构建/运行进行验证。
- 除非用户明确要求，否则避免使用破坏性命令（`reset --hard`、强制清理）。
- 如果环境/网络导致第三方下载失败，先说明备选方案，再在采取高风险绕过方式前征求确认。

## 跨平台变更护栏（强制）
- 本项目为全平台软件；每次改动都必须先做“跨平台影响评估”，再执行最小验证，禁止仅按当前平台结论收尾。
- 交付或收尾时必须明确说明“本次影响面”：`Windows`、`Android`、`iOS`、`macOS`、`Linux`、`Web` 中哪些受影响、哪些逻辑上不受影响；禁止只按当前开发机平台下结论。
- 平台专有行为必须使用**明确条件门控**（如 `TargetPlatform`、`kIsWeb`、专用 UI 分支），避免一端修复拖坏另一端。
- TV / 桌面 / 触控设备交互路径不同时，分层设计，避免手势与焦点/键盘互抢。
- 若改动涉及平台相关能力（插件、`dart:io`、手势/键盘、文件系统、权限、构建脚本、`pubspec` 依赖），至少执行：
  - `flutter analyze`（根工程）；
  - 受影响平台的 `flutter build` 或等效最小可执行验证；
  - 无法在当前环境验证的平台，需在回复中给出“未验证原因 + 用户侧验证命令”；**禁止假装已全平台实机验证**。
- 任何 `third_party`、插件 `example`、`dependency_overrides`、本地 `path` 依赖改动，默认按“高风险跨平台变更”处理：
  - 先检查目录是否真实存在；
  - 再验证不会破坏非当前平台的依赖求解与构建入口。

## 规则与文档一致性
- `.cursor/rules/*.mdc` 中与当前实现不一致的历史描述应及时修正或删除，**以仓库当前代码与最新记录为准**，避免后续 AI 与维护者被误导。

## 外部参考要求
- 当媒体播放行为、流 URL、字幕、转码或鉴权/会话逻辑不明确时，修改代码前先查阅 Emby API 文档。
- 当 `fvp` 的行为/API/原生构建集成不明确时，修改代码前先检查 `fvp` 源码（包源码和/或插件 CMake/原生桥接实现）。
- 对依赖外部文档/源码的修复，需要在回复中记录“查阅了什么、为什么查阅”（或按要求记录到项目说明/变更日志），以便后续变更可追溯。

## 参考链接（优先）
- Emby API 文档：`https://dev.emby.media/reference/RestAPI.html`
- Emby API 示例：`https://github.com/MediaBrowser/Emby/wiki/Api`
- fvp 包页面：`https://pub.dev/packages/fvp`
- fvp 源码仓库：`https://github.com/wang-bin/fvp`
- mdk-sdk wiki（fvp 后端细节）：`https://github.com/wang-bin/mdk-sdk/wiki`
