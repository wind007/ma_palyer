---
description: 2026-04-21 请求频率与风险收敛记录（中文）
alwaysApply: false
---

# 今日工作记录（请求风险治理）

## 目标
- 检查并收敛可能造成高频请求/服务端压力放大的调用路径。
- 优先处理会形成“请求风暴”的逻辑，再处理稳定高频上报和详情页突发请求。

## 已完成改动

### 1) 401 自动重试风暴收敛（`lib/services/emby_api.dart`）
- 在 `_request(...)` 新增 `authRetryCount` 参数。
- 新增认证重试上限：`_maxAuthRetryCount = 1`。
- 遇到 `401` 时：
  - 超过上限直接抛错，避免无限递归重试；
  - 重试前增加 `300ms` 短退避，降低服务端瞬时压力。

### 2) 播放进度上报降频与去重（`lib/pages/video_player_page.dart`）
- 周期上报从 `30s` 调整为 `60s`。
- 增加最小位移阈值：`_minProgressReportDeltaSeconds = 10`（位置变化不足阈值不发请求）。
- 增加并发互斥：`_isReportingProgress`，避免上一次上报未完成时重复并发。
- 增加最近上报位置缓存：`_lastReportedPositionTicks`，用于判定是否需要上报。
- 抽出 ticks 常量：`_ticksPerSecond`，统一换算。

### 3) 剧集详情页 N+1 请求改为按季懒加载（`lib/pages/tv_show_detail_page.dart`）
- 移除“进入详情后按季循环一次性拉全量剧集”逻辑。
- 新增缓存与状态：
  - `_seasonEpisodes`：已加载季缓存；
  - `_seasonLoading`：季加载中标记，避免重复并发请求。
- 页面初次仅拉详情与季列表；默认加载首季剧集。
- 切换季时再请求该季剧集；若已缓存则直接复用。
- 当前季加载中时显示加载态，避免误操作。

## 风险收益
- 避免 `401` 持续失败时的无限重试链路，显著降低请求风暴风险。
- 播放场景写请求频率与并发重叠减少，降低长时播放对服务端写压。
- 电视剧详情页由“进入即突发多请求”改为“按需请求”，削峰明显。

## 建议验证
- 认证异常场景（手动使 token 失效）下，确认不会出现无限重试。
- 连续播放 10 分钟，观察进度上报频率是否按预期下降且暂停/退出上报仍正常。
- 打开多季剧集详情，确认只在切换到对应季时发起请求，已加载季不重复请求。

## 后续可选优化
- 增加全局请求并发上限（同域并发池）与按 endpoint 节流。
- 为关键请求引入统一退避策略与可观测日志（请求速率、失败率、重试次数）。
